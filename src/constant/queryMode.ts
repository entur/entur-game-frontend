import { QueryMode } from '@entur/sdk'

export const ALL_MODES: QueryMode[] = [
    QueryMode.FOOT,
    QueryMode.BUS,
    QueryMode.TRAM,
    QueryMode.RAIL,
    QueryMode.METRO,
    QueryMode.WATER,
]
