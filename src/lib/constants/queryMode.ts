import { QueryMode } from '@entur/sdk'

export const ALL_MODES: QueryMode[] = [
    QueryMode.BUS,
    QueryMode.METRO,
    QueryMode.TRAM,
    QueryMode.RAIL,
    QueryMode.WATER,
    QueryMode.COACH,
]
